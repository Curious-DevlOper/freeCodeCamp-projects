// const myArray = [["John", 23], ["dog", 3]];

// Add elements to the beginning of the array
// myArray.shift();       // Adds 1 to the beginning
// myArray.unshift(["Paul", 35]);   

const dogs = {
    Fido: "Mutt",
    Hunter: "Doberman",
    Snoopie: "Beagle"
  };
  
  const myDog = "Hunter";
  const myBreed = dogs[myDog];
  console.log(myBreed);